### Application to connect to supabase database

Also you need to install this dependencies

```
npm install @supabase/supabase-js dotenv express morgan nodemon
```

Add this in package.json

```
 "scripts": {
    "dev": "nodemon app.js"
  }
```
